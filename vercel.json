{
  "framework": "vite",
  "buildCommand": "cd trading-ui && npm install && npm run build",
  "outputDirectory": "trading-ui/dist",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
